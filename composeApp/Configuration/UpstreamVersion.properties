# The upstream Nuvio Desktop release this fork last merged. StreamCut's own
# version lives in DesktopVersion.properties, which upstream also writes, so
# the "Based on Nuvio Desktop <version>" credit in About reads from here.
# scripts/upstream/sync-upstream.sh rewrites this file on every sync.
UPSTREAM_VERSION_NAME=0.1.25-alpha
UPSTREAM_COMMIT=f2f42a64970455314fd70fde8c2c3c618d28c479
