# Paths StreamCut owns outright. After an upstream merge, sync-upstream.sh resets
# each of these to StreamCut's version whether or not git reported a conflict,
# so upstream edits never leak in silently. Git pathspec globs, one per line.

README.md
PRIVACY.md
composeApp/Configuration/DesktopVersion.properties
.github/workflows/desktop-release.yml

# Branding: regenerated by branding/make_brand.py, never taken from upstream.
composeApp/src/desktopMain/resources/icons/**
composeApp/src/commonMain/composeResources/drawable/app_icon_*
composeApp/src/commonMain/composeResources/drawable/app_brand_mark.png
composeApp/src/commonMain/composeResources/drawable/app_logo_wordmark*
